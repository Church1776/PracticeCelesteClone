#ifndef CPP_ITERATOR_HPP
#define CPP_ITERATOR_HPP

#include <iterator>

namespace cpp::iterator {
    using std::begin;
    using std::end;
    using std::next;
    using std::prev;
    using std::distance;
    using std::advance;
    using std::iterator_traits;
    using std::cbegin;
    using std::cend;
}

#endif//CPP_ITERATOR_HPP